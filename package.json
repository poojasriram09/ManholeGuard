{
  "name": "manholeguard",
  "version": "1.0.0",
  "private": true,
  "description": "Predictive QR-based digital safety system for sanitation workers",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "deploy:hosting": "turbo run build --filter=@manholeguard/dashboard --filter=@manholeguard/worker-app --filter=@manholeguard/citizen-portal && firebase deploy --only hosting",
    "emulators": "firebase emulators:start"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=20.0.0"
  }
}
